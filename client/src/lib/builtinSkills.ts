/** 内置技能：首次启动 seed 到 ~/.yt/skills/<名>/SKILL.md
 * 这些技能改编自 OpenClaw（初号机）的技能库，适配 YT 桌面版的工具集
 * （run_shell / read_file / write_file / edit_file / glob / grep / web_search / web_fetch / 插件）。
 */
export interface BuiltinSkill {
  name: string;
  content: string;
}

export const BUILTIN_SKILLS: BuiltinSkill[] = [
  {
    name: "docx-report",
    content: `---
name: "docx-report"
description: "从原始材料(日志/表格/文本/旧报告)生成专业 Word 报告：数据驱动、分段生成、含图表与截图位"
---

# 技能：生成专业 Word 报告

## 适用场景
用户给一批材料，要求整理成一份结构清晰的 Word(.docx) 报告。如项目总结、分析报告、方案、调研。

## 前置检查
先用 run_shell 确认环境：
- \`python -c "import docx; print(docx.__version__)"\`，缺失则 \`pip install python-docx\`
- 要画图表另需 matplotlib：\`pip install matplotlib\`

## 标准流程
1. **吃透材料**：用 read_file / grep 把材料读全，先打印关键数据摘要（总量、时间范围、TOP 项），确认理解无误再动笔。数据驱动，不编造。
2. **设计目录**：按实际内容组织章节树，不照搬模板。
3. **分段生成（关键）**：把生成拆成多个小脚本，每段写一章、单独 run_shell 执行，每段跑完就 doc.save()，下段重新 Document(已存文件) 续写。避免一个超长任务中断。
4. **中文字体**：docx 正文设置 eastAsia 字体为"宋体"，标题可用"黑体"。matplotlib 画图若中文变方块，需换含中文+数字的字体(如 Noto Sans CJK SC)。
5. **图表/截图位**：需要配图处，用单格表格插入灰色斜体提示「【此处插入截图：XXX】」，给用户预留位置；不要留空段让用户猜。
6. **校验交付**：重新打开成品，打印段落数/表格数/图片数确认结构完整。

## 常见坑
- 长任务中断 → 必须分段脚本化。
- 中文字体缺数字字形 → 换 Noto Sans CJK SC。
- 用 python 处理中文，避免 shell/sed 转义问题。

## 交付
成品 .docx 用绝对路径写到用户指定或桌面目录；告诉用户需手动处理项（贴截图、按 F9 更新目录、脱敏）。
`,
  },
  {
    name: "file-organizer",
    content: `---
name: "file-organizer"
description: "整理本地文件夹：按类型/日期归类、批量重命名、找重复文件、清理临时文件"
---

# 技能：本地文件整理

## 适用场景
用户要整理某个文件夹（下载目录、桌面、项目文件夹等）。

## 原则（重要）
- **先看后动**：先用 list_dir / glob 列清楚有什么，向用户汇报计划，得到确认再执行移动/删除。
- **删除慎重**：能移动到"待删除"子目录就不要直接 rm。真要删走 run_shell（高危，会弹审批）。
- **可回退**：批量操作前，把原始清单写到一个 txt 备查。

## 标准流程
1. **盘点**：\`glob\` 找出所有文件，\`list_dir\` 看结构；用 run_shell \`ls -la\` 或 \`du -sh\` 看大小。
2. **分类计划**：按扩展名/日期/大小归类，先把"计划怎么归"讲给用户听。
3. **确认后执行**：用 run_shell 的 mkdir / mv 归类。批量重命名用脚本，先 dry-run 打印将要执行的命令。
4. **找重复**：对可疑重复文件，用 hash 插件或 run_shell \`md5sum\` 比对，同 hash 才算重复。
5. **汇报**：整理完列出"移动了什么、还剩什么、建议删什么"。

## 常见坑
- 中文文件名 → run_shell 命令里用引号包裹路径。
- 移动前确认目标目录已建（mkdir -p）。
`,
  },
  {
    name: "web-research",
    content: `---
name: "web-research"
description: "联网调研某个主题并输出结构化汇总：多源搜索、抓取正文、交叉验证、给出带出处的结论"
---

# 技能：联网调研与汇总

## 适用场景
用户要了解某个主题/产品/事件的最新情况，需要联网查证并给出可靠汇总。

## 标准流程
1. **拆解问题**：把大问题拆成几个具体的搜索点。
2. **多源搜索**：用 web_search 对每个点搜索；不要只信第一条，看多个来源。
3. **抓取正文**：对关键结果用 web_fetch 读全文，不要只看搜索摘要。
4. **交叉验证**：多个来源一致才当作可靠结论；有冲突要指出。
5. **结构化输出**：
   - 先给一句话结论
   - 分点列关键信息
   - **每个关键结论标注来源**（网址或来源名）
   - 有不确定的地方明确说"未能确认"
6. **时效性**：涉及"最新/现在"的信息，注意结果的发布日期，优先近期来源。

## 原则
- 不编造。查不到就说查不到，别硬凑。
- 区分"事实"与"观点/推测"。
- 用户要深入某点时，再针对性 web_fetch 抓取。
`,
  },
];
